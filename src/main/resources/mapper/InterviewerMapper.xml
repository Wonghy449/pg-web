<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pixel.pgback.mapper.InterviewerMapper">
    <resultMap id="BaseResultMap" type="com.pixel.pgback.entity.Interviewer">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="first_name" jdbcType="VARCHAR" property="firstName"/>
        <result column="last_name" jdbcType="VARCHAR" property="lastName"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="location" jdbcType="VARCHAR" property="location"/>
        <result column="level" jdbcType="VARCHAR" property="level"/>
        <result column="support_city" jdbcType="VARCHAR" property="supportCity"/>
        <result column="interview_type" jdbcType="VARCHAR" property="interviewType"/>
        <result column="interview_area" jdbcType="VARCHAR" property="interviewArea"/>
    </resultMap>
    <insert id="batchInsert" parameterType="list">
        insert into interviewer (first_name, last_name, email, location, level, function)
        values
        <foreach collection="list" separator="," item="record">
            (#{record.firstName},#{record.lastName},#{record.email},#{record.location},#{record.level},#{record.function})
        </foreach>
    </insert>

    <update id="updatePreferredCity">
        update interviewer
        <set>
            <if test="id != null">
                id = #{id,jdbcType=INTEGER},
            </if>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="workNo != null">
                work_no = #{workNo,jdbcType=VARCHAR},
            </if>
            <if test="email != null">
                email = #{email,jdbcType=VARCHAR},
            </if>
            <if test="location != null">
                location = #{location,jdbcType=VARCHAR},
            </if>
            <if test="level != null">
                level = #{level,jdbcType=VARCHAR},
            </if>
            <if test="identity != null">
                identity = #{identity,jdbcType=VARCHAR},
            </if>
            <if test="preferredCity != null">
                preferred_city = #{preferredCity,jdbcType=VARCHAR},
            </if>
        </set>
        where id = #{id}
    </update>
    <update id="uadateBatchArea">
        update interviewer set interview_area = #{area}
        where id in
        <foreach collection="ids" open="(" close=")" separator="," item="item" index="index">
            #{item}
        </foreach>
    </update>
    <select id="selectRegister" resultType="com.pixel.pgback.entity.Interviewer">
        select *
        from interviewer
        where email = #{eamil}
    </select>
    <select id="selectAllInterviewer" resultType="java.util.Map">
        select e.id,e.first_name, e.last_name, e.email, e.level, e.location, e.support_city
        from interviewer e
        <where>
            <if test="name != null">
                CONCAT(first_name,last_name) LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="level != null">
                and e.level in
                <foreach collection="level" open="(" close=")" separator="," item="item" index="index">
                    #{item}
                </foreach>
            </if>
            <if test="location != null">
                and e.location in
                <foreach collection="location" open="(" close=")" separator="," item="item" index="index">
                    #{item}
                </foreach>
            </if>
        </where>
    </select>
    <select id="mySelectBatchIds" resultType="com.pixel.pgback.entity.Interviewer">
        select * from interviewer where id in
        <foreach collection="list" open="(" close=")" separator="," item="item" index="index">
            #{item}
        </foreach>
    </select>
</mapper>